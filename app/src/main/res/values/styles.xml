<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- 解决启动页白屏的问题 -->
    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>


    <style name="SlideCloseTheme" parent="AppTheme">
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">?android:windowNoTitle</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>


    <style name="SDialog" parent="Theme.AppCompat.Light.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- Define window properties as desired -->
    </style>

    <style name="SLightDialog" parent="Theme.AppCompat.Light.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- Define window properties as desired -->
    </style>


    <!--默认动画-->
    <style name="anim_one_dialog">
        <item name="android:windowEnterAnimation">@anim/one_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/one_dialog_exit</item>
    </style>

    <!-- 缩放动画 -->
    <style name="ScaleAnimStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_scale_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_scale_out</item>
    </style>

    <!-- ios 动画 -->
    <style name="IOSAnimStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_ios_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_ios_out</item>
    </style>

    <!-- 顶部弹出动画 -->
    <style name="TopAnimStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_top_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_top_out</item>
    </style>

    <!-- 底部弹出动画 -->
    <style name="BottomAnimStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_bottom_out</item>
    </style>

    <!-- 左边弹出动画 -->
    <style name="LeftAnimStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_left_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_left_out</item>
    </style>

    <!-- 右边弹出动画 -->
    <style name="RightAnimStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_right_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_right_out</item>
    </style>

    <!--右边弹出缩放动画-->
    <style name="RightScaleAnimStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/popup_scale_right_in</item>
        <item name="android:windowExitAnimation">@anim/popup_scale_right_out</item>
    </style>

    <!--左边弹出缩放动画-->
    <style name="LeftScaleAnimStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/popup_scale_left_in</item>
        <item name="android:windowExitAnimation">@anim/popup_scale_left_out</item>
    </style>

    <style name="MyTipDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!--默认字体样式-->
    <style name="DialogCustomTextStyle" parent="Widget.AppCompat.TextView">
        <item name="autoSizeMaxTextSize">@dimen/dialog_comment_size</item>
        <item name="autoSizeMinTextSize">@dimen/dialog_comment_min_size</item>
        <item name="autoSizeTextType">uniform</item>
        <item name="android:textColor">@color/dialog_content</item>
        <item name="android:textSize">@dimen/dialog_comment_size</item>
    </style>

    <!--单行字体样式-->
    <style name="DialogSignalAutoLineTextStyle" parent="DialogCustomTextStyle">
        <item name="android:maxLines">1</item>
    </style>
</resources>